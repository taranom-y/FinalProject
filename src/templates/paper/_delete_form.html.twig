<form method="post" action="{{ path('app_paper_delete', {'id': paper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paper.id) }}">
    <button class="btn">Delete</button>
</form>
