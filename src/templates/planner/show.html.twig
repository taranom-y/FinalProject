{% extends 'base.html.twig' %}

{% block title %}Planner{% endblock %}

{% block body %}
    <h1>Planner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ planner.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ planner.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ planner.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ planner.type }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ planner.image }}</td>
            </tr>
            <tr>
                <th>ImageFile</th>
                <td>{{ planner.imageFile }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ planner.deletedAt ? planner.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planner_index') }}">back to list</a>

    <a href="{{ path('app_planner_edit', {'id': planner.id}) }}">edit</a>

    {{ include('planner/_delete_form.html.twig') }}
{% endblock %}
